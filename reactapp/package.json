{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "rc-slider": "^8.6.1",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && mv build .. && mv ../build ../client",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
